[project]
name = "sample"
version = "0.1.0"
description = "Sample simulator for the app template"
requires-python = ">=3.11"
dependencies = [
    "pylogix>=1.0.7",
]

[project.scripts]
doover-app-run = "main:main"
